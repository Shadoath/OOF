<%= form_for(@plant_status) do |f| %>
  <% if @plant_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plant_status.errors.count, "error") %> prohibited this plant_status from being saved:</h2>

      <ul>
      <% @plant_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
