# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<% 10.times do |n| %>
plant_<%= n %>:
  name: <%= "plant#{n}" %> 
  plant_type: <%= "type#{n}" %>
  harvest_type_id: <%= n %>
  plant_group: <%= "group#{n}" %>
  water_amount_id: <%= n %>
  comment: <%= "comment #{n}" %>
<% end %>